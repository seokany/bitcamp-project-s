<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="bitcamp.java89.ems2.dao.MemteeDao">

  <resultMap type="mentee" id="mentee-map">
    <id     column="mno"        property="memberNo"/>
    <result column="email"      property="email"/>
    <result column="name"       property="name"/>
    <result column="pwd"        property="password"/>
    <result column="age"        property="age"/>
    <result column="mimg"       property="photoPath"/>
  </resultMap>
  
  <select id="getList" resultMap="member-map">
    select 
      mno, 
      email, 
      name, 
      age
    from memb
  </select>
  
  <select id="getOneByEmailPassword" parameterType="map" resultMap="member-map">
    select 
      mno, 
      name, 
      age, 
      email
    from memb
    where email=#{email} 
      and pwd=password(#{password})
  </select>
  
  <select id="getOneByNo" parameterType="int" resultMap="member-map">
    select 
      mno,
      email, 
      name, 
      age,
      path
   from memb
   where mno=#{value}
  </select>
  
  <select id="countByNo" parameterType="int" resultType="int">
    select count(*)
    from mentee s
      left outer join memb m on s.sno=m.mno
    where s.uno=#{value}
  </select>
  
  <select id="countByEmail" parameterType="String" resultType="int">
    select count(*)
    from mentee s
      left outer join memb m on s.sno=m.mno
    where m.email=#{value}
  </select>
  
  <insert id="insert" parameterType="mentee">
    insert into mentee(email, name, age, pwd, path)
    values(#{email}, #{name}, #{age}, password(#{password}),"default.jpg")
  </insert> 
  
  <update id="update" parameterType="mentee">
    update memb set
      email=#{email}, 
      name=#{name}, 
      pwd=password(#{password}), 
      age=#{age}
    where mno=#{memberNo}
  </update> 
  
  
  
  
  
  
  
  
  <select id="count" parameterType="string" resultType="int">
    select count(*) 
    from memb 
    where email=#{value}
  </select>
  
  <select id="countByEmailPassword" parameterType="map" resultType="int">
    select count(*) 
    from memb 
    where email=#{email} 
      and pwd=password(#{password})
  </select>
  
  
  <select id="getOne" parameterType="string" resultMap="member-map">
    select 
      mno, 
      name, 
      age
    from memb
    where email=#{value}
  </select>
  

  
  <delete id="delete" parameterType="int">
    delete from memb where mno=#{value}
  </delete>
  
  
  
  
 

  <update id="updateImg" parameterType="map">
    update MEMB
    set
      PATH=#{photoPath}
    where MNO=#{memberNo}
  </update>
  
</mapper>








